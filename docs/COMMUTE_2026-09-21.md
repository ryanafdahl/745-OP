# September 21, 2026 commute review

Collected comma rlogs/qlogs and Jetson retained system/server logs. Raw logs remain private. No driving software, power profile or safety/steering settings were changed.

## Coverage

Seven recordings, 231 full-rate segments: **108.63 miles / 174.83 km**, **3.80 recorded hours**. This includes the commute, shorter trips and parked time; distance is integrated vehicle speed, not a GPS route reconstruction.

| Recording (UTC start) | Minutes | Miles | Large / total model messages |
| --- | ---: | ---: | ---: |
| 12:45:24 | 0.88 | 0.00 | 0 / 791 |
| 12:48:09 | 3.71 | 0.81 | 2,217 / 4,197 |
| 12:54:44 | 59.77 | 46.29 | 34,750 / 71,523 |
| 13:58:19 | 3.10 | 0.77 | 0 / 3,510 |
| 17:59:34 | 28.63 | 4.67 | 0 / 34,080 |
| 18:31:50 | 16.37 | 6.61 | 0 / 19,407 |
| 22:46:44 | 115.51 | 49.49 | 0 / 138,308 |

## Model and USB findings

Large-model messages: **36,967/271,816 (13.60%)**, all 36,967 valid. Sample-weighted large-model execution: **38.71 ms**. These are message proportions, not a claim of continuous Jetson availability.

The morning main recording covered 46.29 miles. Large-model operation began about nine minutes after its first native model message, ran for 28m58s, then fell back at **13:32:52 UTC / 06:32:52 PDT**, approximately **68 mph**. The exception was `host dropped the gadget configuration (udc: not attached)` and Type-C detection reported **CC 0**. No later large-model rejoin occurred in that recording. Whether the disconnection was intentional is awaiting owner confirmation. It must not be counted as the same brief reset-and-recovery incident seen on September 20.

The Jetson retained session with approximately 34,646 processed frames near its final warning is consistent with this 34,750-message large-model interval. It loaded BMRLNAP `a086d5249fc308bb`. Retained recent boot sections contain no new LIBUSB_ERROR burst, SuperSpeed reset or kernel-panic entry; abrupt power loss or incomplete logging can leave no final error, so absence of those messages does not establish uninterrupted operation.

The main outbound and return trips total **95.78 miles**. The **49.49-mile / 1h55m31s return trip** recorded **138,308 native-model messages, zero large-model messages and no Jetson telemetry**. It does not provide a return-trip Jetson endurance measurement.

## Available Jetson telemetry

**1,747 samples**, with 0 reporting dead=true. Missing intervals are unavailable, not healthy or zero. These BMRLNAP observations remain separate from Cinque Terre.

| Measurement | Sample-weighted mean | Peak |
| --- | ---: | ---: |
| Temperature | 57.29 C | 59.80 C |
| Power | 12.64 W | 13.22 W |
| GPU load | 89.46 % | 100.00 % |
| GPU clock | 918.00 MHz | 918.00 MHz |
| Fan | 3701.68 RPM | 4125.00 RPM |

The internal rail reading is not the 20 V barrel input. Selected Jetson slow-frame warnings (90, not whole-drive samples) averaged 23.39 ms GPU, 25.12 ms processing and 14.20 ms send; maximum send was 69.7 ms.

Across the two prior BMRLNAP drives and today's two recordings with telemetry, **3,651 samples** yield descriptive weighted means of **59.13 C, 12.85 W, 85.09% GPU and 4,006 RPM**. Historical GPU clocks differ; this is not a controlled performance comparison. Cinque Terre remains separate.

## Steering and comma process health

Temporary steering-fault flag time: **126.06 s** across the recordings. Permanent steering-fault time: **0.00 s**; CAN timeout time: **0.00 s**. Today's aggregate flags have not been individually re-decoded to EPS status; the prior day's NO_TORQUE_ALERT_1 interpretation must not automatically be applied to every new episode.

The outbound drive recorded process-communication and model-lag soft-disable alerts in addition to the USB loss. Closely aligned diagnostic messages identify missing driverMonitoringState, low-rate deviceState, and a planner-message interruption. Other low-rate alerts occurred on native-model operation. Optional/ignored alertDebug, lateralManeuverPlan and SP streams also appear in diagnostic lists and must not be mistaken for the sole fault cause. Alert transitions are not necessarily independent incidents.

Across recordings there were **10 model-message intervals above 100 ms**. This and the communication alerts need follow-up; valid large-model messages alone do not qualify the complete system as uninterrupted.

The native-only return trip had **3 communication, 4 low-rate communication, 1 model-lag and 1 locationd soft-disable alert transitions**. Its largest model-message gap was **817.70 ms**. These counts are alert transitions, not necessarily separate root-cause incidents, and establish that not every reliability alert requires an active Jetson link.

## Jetson startup and management

The retained logs include a power-profile startup failure (golden-context warning, exit 234) and subsequent successful startup. Offline clocks jump between 2026 and 1970; precise assignment of that failed boot to a particular trip is unresolved. Do not interpret 00:08:xx wall timestamps as measured boot duration.

At collection, nvpmodel and Jetlink were active with exit status 0; **25 W**, GPU maximum **918 MHz**, and fan profile **cool** were confirmed. Comma runtime remained `aa2fd69ba4ce1a5e7d716248e281d4dea8d853a6`.

**Management now uses Ethernet.** The owner removed the USB Wi-Fi adapter; Jetson was accessed at `192.168.1.187`. No Wi-Fi configuration or driver packages were removed during this collection.

## Next investigations

1. Confirm whether the 06:32:52 PDT Jetson disconnection was intentional. If not, investigate the power/USB path using stationary paired captures.
2. Investigate driver-monitoring/device-state/planner scheduling and message health on the comma without disabling their checks.
3. Reproduce and capture the intermittent Jetson power-profile startup failure on the bench; the current successful boot does not erase the earlier failure.

## Evidence and method

Full-rate rlog carState samples supply time-weighted durations and integrated distance. Model execution and telemetry means are weighted by sample count; averages of per-route percentiles are not used. Current comma journal covers its latest boot, so historical conclusions primarily use saved drive logs. Jetson syslog retention and offline clock jumps limit exact cross-device correlation.

Private archives were downloaded and verified against device SHA-256 hashes:

- Comma rlog/qlog archive: `dd1b2df0815f33a43179c1ee3d171d54a7a5c2514c5a86b9a5ad8376262b82d5`.
- Jetson logs: `f297cc70357b0b6584af515bf236e63da252100c4ad74573f49a612ecd78a151`.

